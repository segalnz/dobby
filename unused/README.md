# Historical material

`server_specification_v1.txt` describes the superseded SyncStreamer v1 wire format.
It is retained as design history, not setup instructions. The root README and
`syncstreamer/README.md` describe the current implementation.

Alternative voice/output modules remain in their original locations because they
may be used by other output types. They have not been classified as disposable.
